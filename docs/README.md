# 📖 مستندات پروژه هاویرکشت

## فهرست مطالب

1. [معرفی پروژه](#معرفی-پروژه)
2. [معماری و ساختار](#معماری-و-ساختار)
3. [فناوری‌ها](#فناوریها)
4. [راهنمای نصب و راه‌اندازی](#راهنمای-نصب-و-راهاندازی)
5. [راهنمای API](#راهنمای-api)
6. [صفحات و امکانات](#صفحات-و-امکانات)
7. [راهنمای توسعه](#راهنمای-توسعه)
8. [استقرار](#استقرار)

---

## معرفی پروژه

**هاویرکشت** یک سامانه مدیریت کشت قراردادی چغندر قند است که برای مدیریت قراردادها، کشاورزان، تعهدات و صورتحساب‌ها طراحی شده است.

### ویژگی‌های اصلی:
- 🌱 مدیریت سال‌های زراعی
- 📍 مدیریت استان‌ها و مناطق
- 👨‍🌾 ثبت و مدیریت قراردادهای کشاورزان
- 📋 مدیریت تعهدات
- 💰 صدور و مدیریت صورتحساب‌ها
- 👥 مدیریت کاربران (ادمین، پیمانکار، کشاورز، راننده)

---

## معماری و ساختار

### ساختار پوشه‌ها

```
WebFinalProject/
├── index.html              # صفحه لاگین
├── package.json            # تنظیمات npm
├── tailwind.config.js      # تنظیمات Tailwind CSS
│
├── css/
│   ├── input.css           # فایل ورودی Tailwind
│   ├── output.css          # CSS کامپایل شده
│   ├── custom.css          # استایل‌های سفارشی
│   └── fonts.css           # تعریف فونت‌ها
│
├── fonts/
│   └── Vazirmatn-*.woff2   # فایل‌های فونت وزیرمتن
│
├── js/
│   ├── config.js           # تنظیمات اصلی و آدرس API
│   ├── api.js              # ماژول ارتباط با API
│   ├── auth.js             # مدیریت احراز هویت
│   ├── utils.js            # توابع کمکی
│   ├── login.js            # منطق صفحه لاگین
│   ├── dashboard.js        # منطق داشبورد
│   ├── crop-year.js        # مدیریت سال زراعی
│   ├── province.js         # مدیریت استان
│   ├── farmer-contracts.js # قراردادهای کشاورزان
│   ├── commitment.js       # مدیریت تعهدات
│   ├── farmer-invoice.js   # صورتحساب کشاورزان
│   ├── invoice-farmer.js   # گزارش فاکتور
│   ├── manage-users.js     # مدیریت کاربران
│   └── sidebar.js          # منوی کناری
│
├── pages/
│   └── admin/              # صفحات پنل ادمین
│       ├── dashboard.html
│       ├── crop-year.html
│       ├── province.html
│       ├── farmer-contracts.html
│       ├── commitment.html
│       ├── farmer-invoice.html
│       ├── invoice-farmer.html
│       └── manage-users.html
│
└── docs/                   # مستندات
    ├── README.md
    ├── API.md
    ├── DEPLOYMENT.md
    └── DEVELOPMENT.md
```

### الگوی معماری

این پروژه از معماری **کلاینت-سایدی (Client-Side)** استفاده می‌کند:

- **Frontend**: HTML + Tailwind CSS + Vanilla JavaScript
- **Backend API**: FastAPI (Python) - خارج از این پروژه
- **ارتباط**: REST API با JWT Authentication

---

## فناوری‌ها

| فناوری | نسخه | کاربرد |
|--------|------|--------|
| HTML5 | - | ساختار صفحات |
| Tailwind CSS | 3.4.x | استایل‌دهی |
| JavaScript (ES6+) | - | منطق برنامه |
| فونت وزیرمتن | 33.003 | فونت فارسی |

---

## راهنمای نصب و راه‌اندازی

### پیش‌نیازها
- Node.js (نسخه 18+)
- npm یا yarn

### مراحل نصب

```bash
# 1. کلون کردن پروژه
git clone <repository-url>
cd WebFinalProject

# 2. نصب وابستگی‌ها
npm install

# 3. کامپایل CSS
npm run build:css

# 4. اجرای سرور توسعه (اختیاری)
python3 -m http.server 3000
# یا با Live Server در VS Code
```

### دستورات npm

```bash
npm run build:css    # کامپایل یک‌باره CSS
npm run watch:css    # کامپایل خودکار هنگام تغییرات
npm run dev          # حالت توسعه (مشابه watch)
```

---

## راهنمای API

آدرس پایه API:
```
https://edu-api.havirkesht.ir
```

### احراز هویت

```javascript
POST /token
Content-Type: application/x-www-form-urlencoded

username=<username>&password=<password>
```

پاسخ:
```json
{
  "access_token": "...",
  "refresh_token": "...",
  "user_id": 1,
  "role_id": 1,
  "fullname": "نام کاربر"
}
```

### نقش‌های کاربری

| role_id | نقش |
|---------|-----|
| 1 | ادمین |
| 2 | پیمانکار |
| 3 | کشاورز |
| 4 | راننده |

### Endpoints اصلی

| متد | مسیر | توضیح |
|-----|------|-------|
| GET | `/farmer/` | لیست کشاورزان |
| POST | `/farmer/` | ایجاد کشاورز |
| PUT | `/farmer/{id}` | ویرایش کشاورز |
| DELETE | `/farmer/{id}` | حذف کشاورز |
| GET | `/province/` | لیست استان‌ها |
| GET | `/crop-year/` | لیست سال‌های زراعی |
| POST | `/report-full/` | گزارش کامل |

---

## صفحات و امکانات

### 1. صفحه لاگین (`index.html`)
- ورود با نام کاربری و رمز عبور
- ذخیره توکن در localStorage
- هدایت به داشبورد پس از ورود موفق

### 2. داشبورد (`pages/admin/dashboard.html`)
- نمایش آمار کلی
- خلاصه وضعیت قراردادها
- دسترسی سریع به بخش‌های مختلف

### 3. مدیریت سال زراعی (`pages/admin/crop-year.html`)
- ایجاد، ویرایش و حذف سال‌های زراعی
- تعیین سال فعال

### 4. مدیریت استان (`pages/admin/province.html`)
- ثبت استان‌های جدید
- ویرایش اطلاعات استان

### 5. قراردادهای کشاورزان (`pages/admin/farmer-contracts.html`)
- ثبت قرارداد جدید
- جستجو و فیلتر قراردادها
- مشاهده جزئیات

### 6. تعهدات (`pages/admin/commitment.html`)
- ثبت تعهدات کشاورزان
- پیگیری وضعیت تعهدات

### 7. صورتحساب (`pages/admin/farmer-invoice.html`)
- صدور صورتحساب
- مشاهده تاریخچه

### 8. مدیریت کاربران (`pages/admin/manage-users.html`)
- ایجاد کاربر جدید
- تغییر نقش کاربران
- غیرفعال کردن حساب

---

## راهنمای توسعه

### ساختار فایل JavaScript

```javascript
// الگوی استاندارد برای هر ماژول
document.addEventListener('DOMContentLoaded', function() {
    // بررسی احراز هویت
    if (!Auth.isAuthenticated()) {
        window.location.href = '/index.html';
        return;
    }
    
    // مقداردهی اولیه
    init();
});

async function init() {
    await loadData();
    setupEventListeners();
}

async function loadData() {
    try {
        const data = await Api.get('/endpoint');
        renderData(data);
    } catch (error) {
        Utils.showToast('خطا در دریافت داده‌ها', 'error');
    }
}
```

### کلاس‌های CSS سفارشی

```css
/* دکمه‌ها */
.btn-primary    /* دکمه اصلی سبز */
.btn-secondary  /* دکمه ثانویه */
.btn-danger     /* دکمه خطر/حذف */
.btn-success    /* دکمه موفقیت */

/* فرم‌ها */
.form-input     /* ورودی متن */
.form-label     /* برچسب */
.form-select    /* انتخابگر */

/* Badge ها */
.badge-success  /* موفق - سبز */
.badge-warning  /* هشدار - زرد */
.badge-danger   /* خطر - قرمز */
.badge-info     /* اطلاعات - آبی */
```

### اضافه کردن صفحه جدید

1. فایل HTML در `pages/admin/` ایجاد کنید
2. فایل JS مربوطه در `js/` بسازید
3. لینک‌های CSS و JS را اضافه کنید:

```html
<link rel="stylesheet" href="../../css/output.css">
<link rel="stylesheet" href="../../css/custom.css">
...
<script src="../../js/config.js"></script>
<script src="../../js/api.js"></script>
<script src="../../js/auth.js"></script>
<script src="../../js/utils.js"></script>
<script src="../../js/your-page.js"></script>
```

---

## استقرار

### استقرار استاتیک

پروژه می‌تواند روی هر سرور استاتیک مستقر شود:

```bash
# کامپایل CSS نهایی
npm run build:css

# آپلود فایل‌ها به سرور
# index.html, css/, js/, fonts/, pages/
```

### استقرار با Nginx

```nginx
server {
    listen 80;
    server_name havirkesht.ir;
    root /var/www/havirkesht;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(css|js|woff2)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}
```

### متغیرهای محیطی

برای تغییر آدرس API، فایل `js/config.js` را ویرایش کنید:

```javascript
const CONFIG = {
    API_BASE_URL: 'https://your-api-server.com',
    // ...
};
```

---

## مشارکت‌کنندگان

برای مشارکت در پروژه:

1. Fork کنید
2. Branch جدید بسازید (`git checkout -b feature/amazing-feature`)
3. تغییرات را Commit کنید (`git commit -m 'Add amazing feature'`)
4. Push کنید (`git push origin feature/amazing-feature`)
5. Pull Request ایجاد کنید

---

## لایسنس

این پروژه تحت لایسنس MIT منتشر شده است.

---

📅 آخرین به‌روزرسانی: فوریه ۲۰۲۶
